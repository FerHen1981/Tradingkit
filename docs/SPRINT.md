# SPRINT.md — het bord

> **De enige lijst met lopend werk.** Elke code-sessie leest dit eerst, claimt één item,
> en commit de claim vóór het werk begint. Vervangt `docs/scrum.md` (twee varianten) en
> de losse itemlijst in `docs/inbox.md`.
>
> Status: `todo` (vrij) · `wip` (in behandeling) · `review` (klaar, wacht op controle) ·
> `done` (goedgekeurd) · `blocked` (wacht op een ander item of op Ferry).
> Owner leeg = vrij te pakken.

**Stand:** 2026-08-20 · geverifieerd tegen `claude/middleware-setup-guide-afhvtk` @ `9830cf3`

---

## Het claim-protocol

1. **Lees dit bestand** vóór je iets anders doet. Werk nooit aan een item dat al `wip`
   staat met een andere owner.
2. **Claim één item:** zet status op `wip`, vul je chatnaam in als owner, en **commit die
   ene regel meteen** — `git commit -m "claim: D-xx — <chatnaam>"` — vóór je aan de inhoud
   begint. Die commit is je slot.
3. **Een merge-conflict op dit bestand is het signaal**, geen fout: een andere sessie pakte
   hetzelfde item. Kies een ander item, forceer niet.
4. **Eén item tegelijk.** Rond af voor je een nieuw item claimt.
5. **Afronden:** wijziging gecommit → status op `review` → regel in `docs/DECISIONS.md`.
   Zet zelf niets op `done`; dat doet de Scrum Master bij de controle.
6. **Nieuwe ID's geeft de Scrum Master uit.** Heb je een nieuw item, zet het in
   `docs/inbox.md`; je krijgt er een D-nummer voor terug. Twee partijen die tegelijk
   het volgende vrije nummer pakken botsen namelijk *niet* in git — dat gebeurde op
   19-08 met D-34 — en dan staan er twee items met hetzelfde ID. `done`-items blijven
   staan als historie.

Raakt je werk iets buiten je eigen map of het live executiepad? Dan geldt daarnaast de
beslissingsboom uit de werkafspraken (`.claude/skills/mex-scrum-master/SKILL.md`).

---

## 🔴 Blokkeert geld of veiligheid

| ID | Status | Owner | Item |
|---|---|---|---|
| **D-52** | done | Scrum Master | ~~Vloot-sweep nog niet verwerkt~~ — **TRIAGE GEDAAN 25-08.** De vier besluitregels van Backtest Setup (`0afb2da`, sweep `0bc13a7`) zijn gescheiden in wat vaststaat en wat niet, en uitgezet als **D-53 t/m D-56**. **Wat vaststaat:** de sizing-bevinding. Die is op mechanisme-niveau vastgesteld, reproduceert over álle engines, en Backtest Setup heeft research-mode aan/uit vergeleken om dubbeltelling uit te sluiten. **Onafhankelijk van de rangorde** — hij blijft staan wat er ook met de poorten gebeurt. → **D-53**. **Wat NIET vaststaat:** de rangorde. Alleen **MATADOR** heeft een gesloten pariteitspoort (`data_parity`); LEON's $17,48 en REY's $13,21 staan onder een **open harde poort**. De besluitregel noemt die cijfers "indicatief", maar de pijplijn is strenger over zichzelf: `state.py` noemt onvervulde harde poorten precies de poorten die downstream-cijfers *"invalid rather than merely early"* maken. **Ik volg de pijplijn, niet de samenvatting.** → **D-54**. **Wat onder voorbehoud staat:** elk MGC-oordeel. PATRON, TESORO en BANDIDO zijn gemeten op de **GC-twin** omdat echte MGC-data ontbreekt — dat geldt ook voor een *afwijzing*. PATRON is dus **niet dood verklaard**, alleen zwaar in twijfel. → **D-56**. **`CLAUDE.md` bijgewerkt:** de oude rangorde is er als ingetrokken uit, met de sweep-tabel, het MGC-voorbehoud en de sizing-bevinding ervoor in de plaats. **Correctie op de samenvatting:** de besluitregel legt het gewicht op de $1.000 DLL, maar het sweep-commit noemt óók de **$2.000 trailing drawdown** van een vers account (verliesreeks ~$2.700 breekt hem voordat de buffer de floor vergrendelt). Dat zijn twee verschillende muren en de trailing-DD is de eerste die je raakt. ⚠️ **Meetvenster gewijzigd:** de sweep lijnt alle engines op `--since 2023-08-24` (3 jaar) om OOM op de 2-core box te vermijden. Cijfers zijn dus niet één-op-één vergelijkbaar met eerdere runs op andere vensters. |
| **D-53** | blocked | Middleware App | **🔴 SIZING: ROUTE B GEKOZEN (Ferry 25-08) — qty per account in de fan-out.** De bevroren volle contractgrootte is niet fresh-account-funderbaar (sweep 25-08): op volle grootte breken **alle** engines op trap 7/8, op 1 contract fundeert **elke** engine. Twee bindende muren: de **$2.000 trailing drawdown** van een vers account (verliesreeks ~$2.700 breekt hem vóór de buffer de floor vergrendelt — dit is de muur die je als eerste raakt) en de **$1.000 DLL** (MATADOR: 6 MES-contracten à $150 stop, één slechte dag = $−1.033). De `.pine`-bron schaalt al in via `derisk`/`deriskPA`; de bevroren config doet dat niet. ⚠️ **CORRECTIE op mijn eigen advies van een uur geleden.** Ik noemde B "omkeerbaar en bijna gratis" met in mijn hoofd de per-account `quantity_multiplier` uit `accounts.example.yaml`. **Die staat in de Python-fan-out, en dat is het dode pad.** De draaiende .NET-receiver leest `accounts.yaml` niet en raakt `quantity` nergens aan — `ForwardJsonAsync` stuurt de body ongewijzigd door. **Er is vandaag géén volumecontrole per account in het live pad.** B blijft de goede keuze, maar het is bouwwerk, geen configregel. **Wat B concreet is:** in `/signal/{token}`, op dezelfde plek als de poort van **D-40**, het account uit `multiple_accounts[0].account_id` opzoeken in een qty-map en vóór `ForwardJsonAsync` het veld `multiple_accounts[0].quantity_multiplier` overschrijven (Pine zet die nu hard op `1` — het is precies de bedoelde haak). Bron: een env-var of klein JSON-bestand, géén nieuwe datafeed. **Bouw D-40 en D-53 samen** — zelfde functie, zelfde plek, één test. **Wat B nog NIET is:** automatisch schalen op accountfase. Dat is v2 en vraagt een echte bron voor de fase (Tradovate-poller). Begin met een handmatige map, vers account = 1. ⛔ **Blokkeert op D-06** — zonder `.sln` en `.csproj` in git kan Middleware App dit niet bouwen. **Zolang dat zo is bestaat er geen code-lever.** Draait er nu iets op volle grootte, dan is de énige knop vandaag de contractgrootte in de TradingView-alert zelf. Dat is per chart in plaats van per account (D-47 laat twee accounts op één chart zien) en het raakt een bevroren parameter — **doe het alleen bewust en leg het vast als tijdelijke afwijking in `DECISIONS.md`.** **⚡ NOG STEEDS ONBEANTWOORD: wat draait er nú en op welke grootte?** De alertlog draagt `qty=` per signaal. Zelfde TradingView-lijst als **D-47**, dus in één keer mee te nemen. Zolang dat onbekend is, is onbekend of er een acuut probleem ís. _(uit de sweep van Backtest Setup 25-08; route B gekozen door Ferry 25-08; eigen advies gecorrigeerd dezelfde dag)_ |
| **D-54** | todo | Backtest Setup + Pine Dev | **De rangorde is pas bruikbaar als de harde poorten van LEON en REY dicht zijn.** Alleen MATADOR heeft `data_parity`; LEON ($17,48/dag) en REY ($13,21/dag) staan onder een **open** harde poort — geen export, of niet gehaald. **Dit is geen formaliteit.** `backtest/pipeline/state.py` zegt het zelf over onvervulde harde poorten: die maken downstream-cijfers *"invalid rather than merely early"*. Een rangorde bouwen op twee ongeldige getallen is exact wat op 23-08 de GC+ES-conclusie de kop kostte. **Behandel $17,48 en $13,21 dus niet als "ongeveer goed".** ⚠️ **TWEEDE CORRECTIE 25-08 — en deze klopt wél.** Ik schreef eerst dat Pine Dev exports moet leveren (onjuist: die bestaan), daarna dat er data ontbrak (óók onjuist: die is geleverd, zie het ingetrokken D-60). **De echte blokkade is D-63:** LEON en REY zijn gevalideerd onder een ándere configuratie dan hun bron draagt, en zolang onduidelijk is tegen welke configuratie gemeten moet worden kan trap 1 niet slagen. Dat is een beslissing voor Ferry, geen aankoop en geen export. **Nodig:** eerst D-63 (besluit per engine), dan trap 1 opnieuw tot de poort `passed` of `data_parity` staat, dan trap 8. Pas daarna mag er een rangorde in `CLAUDE.md`. _(uit de sweep van Backtest Setup 25-08, uitgezet door Scrum Master)_ |
| **D-60** | done | Scrum Master | ⛔ ~~1-minuutdata voor MNQ en MYM ontbreekt~~ — **INGETROKKEN 25-08, dit item had nooit mogen bestaan.** Ferry: *"die datasets heb ik al aangeleverd"*. Klopt, en het stond ook al in de inbox. **Wat ik fout deed:** ik verwerkte inbox 17 (24-08) en nam de datavraag daaruit over als kritiek pad. **Inbox 19 stond er direct onder en heet letterlijk "correctie op inbox 17".** Die had ik moeten lezen vóór ik een P1 uitgaf. **Wat er werkelijk staat:** (a) het venster is niet "jan 2025 →" — dat was `Start date/time (measure from)`, een **entry-onderdrukker, geen tester-range**. De echte `Backtesting range` van alle drie de exports is **24-08-2025 → 23-08-2026**, één jaar per markt. (b) De data ís geleverd, twee keer zelfs: de 20y-twins (`ES/NQ/YM_20y_1m_CVD`, tot 16-08-2026, 8 dagen staart missend = 2,2%, ruim binnen de 10%-tolerantie) én de échte micro-bars (`MES/MNQ/MYM 3y 1m tick_cvd`, ~1,05M bars elk, 2023-08 → 21-08-2026). **Er valt dus niets te kopen.** De spec die ik hier had neergezet (OHLCV volstaat, contractreeks is het risico) blijft juist en bruikbaar voor een volgende dataset, maar er is nu geen aanleiding. **De echte blokkade voor LEON en REY staat in D-63.** _(uitgegeven en dezelfde dag ingetrokken door Scrum Master)_ |
| **D-63** | todo | Pine Dev + Ferry (TradingView) | **✅ BESLIST 25-08 (Ferry): DE BRON WINT, voor beide engines.** Motivering: *"de export is eenmalig"* — de bron staat in versiebeheer, de export is een momentopname. Principieel consistent met hoe dit project bevroren parameters behandelt. **⚠️ De prijs, expliciet:** hiermee vervalt de huidige export als bewijs voor LEON en REY. Trap 1 meet tegen een export, dus **er moet opnieuw gevalideerd worden in TradingView**. ➡️ **Maar het is géén nieuwe onderzoeksronde.** De bevroren parameters veranderen niet — de export moet ze gaan wéérspiegelen. Het is een her-export, geen trap-1-vanaf-nul. **Wat er in TradingView moet worden rechtgezet vóór het exporteren:** **(1) `LEON_MYM_PROD_EOD`** → firm preset op **`apex_50k_eod_pa`** (stond op `apex_50k_intraday_pa`). **(2) `REY_MNQ_PROD_INTRA`** → dag-winstblok op **`Off`** met **500 / 150 / 750** (stond op `Trail + cap` met 750 / 100 / 1000). **(3) 🔑 EN METEEN DE COMMISSIE — anders exporteer je twee keer.** Alle drie de exports draaiden **0,51**, terwijl de registry voor **MNQ, MES én MYM 0,37** draagt. `0,51` ligt verdacht dicht bij MGC's `0,52`: waarschijnlijk staat er een goudwaarde op index-micros, exact het patroon van **D-08**. Zet de commissie op **0,37** vóór de her-export. **Volgorde:** (a) de drie correcties in TradingView · (b) her-export LEON en REY · (c) exports in `validation/exports/` — Backtest Setup heeft die conventie al ingericht omdat bewijs waaraan een harde poort meet in versiebeheer hoort · (d) Backtest Setup draait trap 1, dan trap 8. ⚠️ **Nog te controleren, en het raakt het enige geldige cijfer dat we hebben:** MATADOR haalde `data_parity` terwijl zijn export óók op 0,51 stond. De pariteit slaagde dus ondanks een kostenafwijking — binnen tolerantie, maar wel onder een verkeerde kostenaanname. **Backtest Setup: hertoets MATADOR zodra de commissie gelijkgetrokken is.** Kosten verschuiven PF direct, en $30,59/account-dag is nu het enige cijfer waar iets op gebouwd wordt. _(gemeld door Backtest Setup 24-08 als inbox 19; genummerd en beslist 25-08)_ |
| **D-35** | done | Ferry (VPS) | ~~De dráaiende binary was oud~~ — **AFGEROND 25-08 01:23 UTC, bewijs sluitend.** `BUILD_EXIT=0`, `RESTART_EXIT=0`, `ActiveEnterTimestamp = Tue 2026-08-25 01:23:02 UTC`. De dll bleef op `24-08 11:32:47` staan omdat de build incrementeel was en niets te doen had — en dat is juist het bewijs: de dll is **11 seconden jónger dan `Program.cs`** (`11:32:36`), dus gebouwd uít deze bron, en die bron heeft md5 `1887bbca06d0aa31568d8eb9abf5bc1d` = `0f0e8c5` in de werkbranch. Bron → binary → proces sluiten nu op elkaar aan. **Twee keer eerder vandaag stond dit item verkeerd:** eerst als "gebouwd en herstart" terwijl het proces nog van 23-08 was, daarvoor als "grep zegt 0 treffers" terwijl de checksum het tegendeel bewees. De les die blijft staan: `ActiveEnterTimestamp` is het enige dat over het dráaiende proces gaat, een dll-datum niet. Dat is precies wat **D-31** elk uur gaat vastleggen. **Wat hiermee daadwerkelijk live is gegaan:** IPv4-forcering · body-check (een geweigerde order schrijft `GEWEIGERD` en post "⛔ Order NIET geplaatst" op Discord in plaats van stil te blijven) · D-28's drie hooks gecompileerd maar inert zolang de env-vars niet gezet zijn. |
| **D-01** | done | Middleware App | **`mex-viewer` serveert de hele vloot zonder authenticatie.** `viewer.py:161` faalt open: staat `VIEWER_PASSWORD` niet in de omgeving, dan zijn `/api/state`, `/api/command` en `/api/widget` publiek — accountnummers, saldi, buffers, open posities. **OPGELOST** `8a14537`: `_authed()` faalt nu closed; open draaien vereist een expliciete `VIEWER_ALLOW_OPEN=1` en het dashboard meldt dan *fleet data is PUBLIC*. Vier tests in `middleware/tests/test_viewer_auth.py`. **VOLLEDIG AFGEROND 20-08:** Ferry heeft `VIEWER_PASSWORD` + `VIEWER_API_TOKEN` gezet en mex-viewer herstart. _(was S-08 / SM-01)_ |
| **D-02** | blocked | Ferry + Legacy | **De risk-gate komt in de .NET-receiver** (besluit Ferry 19-08). Day caps, DLL en halt uit `risk.py` worden geport naar `Mex.Journal.Receiver` zodat ze op het live executiepad handhaven. De Python-variant (`risk.py` → `main.py`/`router.py`) vervalt als levend pad. **Blocked by D-06** (broncode moet eerst volledig in git staan). _(was S-01 / SM-02)_ |
| **D-03** | review | Middleware App | **De reconciliatielaag heeft nooit gedraaid.** De Notion-database *MEX Reconciliation (live)* heeft een compleet schema en **0 rijen** (geverifieerd 19-08). Dat verklaart het gat dat Analyses vond: `realized_net` $21.597,35 vs `window_net` $31.424,81 — **$9.827,46** verschil dat precies deze laag had moeten vangen. **Werk gedaan (19-08):** `reconcile_run.py` — bucket op tradingsessie, reconcile op bestaande bronnen, fills-upload-endpoint `/api/upload-fills` + `/upload`-pagina, Notion-creds uit env. **Resteert voor Ferry:** `NOTION_TOKEN` in de service-omgeving zetten + `mex-reconcile.timer` inschakelen zodat hij daadwerkelijk schrijft. _(was S-10 + S-11)_ **EERSTE DATA BINNEN — geverifieerd 24-08:** de database heeft **334 rijen** (was 0). De laag werkt: 6 accounts, correcte fan-out zichtbaar (één MGC-signaal naar zes accounts binnen milliseconden), pairing plausibel. ⚠️ **Maar hij heeft één keer geschreven, op 19-08, en daarna nooit meer** — de timer draait dus nog steeds niet. **Steekproef is klein:** van de 334 rijen dragen er **27** een slippage-meting; 110 andere dragen alleen `Realized $` (de exit-kant). Van die 27: **19 gunstig (≤0 ticks)**, 2 tussen 0–1, en **6 slechter dan 3 ticks à gemiddeld $68,75** — die zes zijn samen vrijwel de volledige $404 slippagekosten. De mediaan is dus gunstig; het gemiddelde wordt door zes fills getrokken. **Het $9.827-gat is hiermee nog niet verklaard** — `Realized $` is te dun gevuld voor een vloot-P&L. **Resteert:** timer inschakelen zodat er dagelijks geschreven wordt, en die zes uitschieters nakijken. |
| **D-04** | done | Ferry | **Notice-cards: de .NET-receiver is de eigenaar — bevestigd door Ferry 20-08.** `notices.py` (discord-notify-branch) is daarmee dood materiaal en gaat niet naar productie. Deblokkeert D-13 (merge-plan) en het .NET-deel van D-28. Feitelijk was dit al beslecht — runtime toont `renderEnabled:true` met `/root/mex-renderer/render-signal.js`; `notices.py` is nooit uitgerold. Alleen de formele bevestiging ontbreekt, en zonder die bevestiging kan niemand mergen. **Eerstehands bewijs (Legacy, uitrol 11-08):** audit-log `card queued (tier B)` 01:28:52Z → `card sent 200` 01:28:57Z, kaart verscheen in Discord; een volledige tradecyclus liep erdoorheen (FILL 6ct @ 4474,8 → RISK OFF → TRAIL → EXIT +$153,96). De Python-tak bleek dood toen patches daar geen effect op de executie hadden. |
| **D-05** | blocked | Middleware App | **Python fan-out afvoeren — SAMENGEVOEGD MET D-02** (besluit Ferry 25-08). Stap 1 is klaar: de zes bestanden dragen een DEAD PATH-header (`19a47d2`). Stap 2 — fysiek verwijderen — gebeurt **in dezelfde ronde als de afwikkeling van D-02**, niet als losse actie. Zolang de risk-gate niet in de .NET-receiver zit blijft `risk.py` de enige plek waar die logica staat. |
| **D-40** | blocked | Middleware App | **🔺 PRIORITEIT VERHOOGD (Ferry 25-08).** ✅ **RICHTING BEPAALD 25-08 (Ferry): handhaving in de fan-out, níét in Pine.** ⚠️ **CORRECTIE 25-08 op dít item — mijn eigen eerdere lezing was fout.** Ik schreef vanochtend dat `pmtBlock` alleen de payout-cap dekt en dat day-cap en DLL daarmee "exact het gat" waren. **Onjuist.** Pine handhaaft day-cap en DLL wél, alleen een laag eerder en vollediger: `dayHalted` (regel 972-973, gevoed door `dllHit`, `dayCapHit`, `dayTrailHit`, `lockHit`) gaat via `canTrade` (regel 1065) rechtstreeks naar `long0`/`short0` (regel 1090-1091). **Een gehalteerd script produceert helemaal géén entry-signaal** — er valt niets te blokkeren bij `f_sendExec`, want er komt niets aan. `pmtBlock` bestaat juist voor het énige geval waarin de chart wél door mag simuleren maar niet mag sturen: de payout-cap. Dat klopt zoals het is. **Het echte gat is dus een ander:** Pine's `dayHalted` rekent op `strategy.netprofit` — **de simulatie**. Op 24-08 boekte TradingView +$759,20 op een trade die bij de broker niet bestond; die $759 telt mee in Pine's DLL-som en de echte accountstand niet. Daarbij is Pine-state per chart terwijl accounts per alert zijn (D-47: PA017 qty 4 en PA018 qty 6 op één chart), dus één `dayHalted` kan nooit twee accountstanden dragen. **D-40 dupliceert Pine niet — het handhaaft de échte stand, en dat kan alleen de fan-out.** Pine blijft daarom volledig ongewijzigd. **⛔ WAT DIT NU BLOKKEERT — twee dingen, allebei bij Ferry:** **(1) D-06** — zonder `.sln`, `.csproj` en `Mex.Journal.Recon` in git kan Middleware App de receiver niet bouwen of testen. **(2) De bron van de blokkadestand is nog onbekend.** De receiver heeft vandaag géén bron van echte accountstand. `Rejected()` (regel 286) leest PMT's antwoord al, maar de tien markers zijn een gok — het codecommentaar zegt het zelf: *"deze lijst is aan te scherpen zodra we het echte antwoordformaat van PMT hebben gezien"*. **Nodig: één echte PMT-weigering.** Op de VPS: `grep -h GEWEIGERD /root/…/routed_*.jsonl | tail -3`. ⚠️ Dat werkt pas ná de **D-35**-build — de body-check zit in de bron maar niet in de dráaiende binary, dus vandaag schrijft hij `GEWEIGERD` nog nergens weg. **Ketting: D-35 → weigering opleveren → D-06 → D-40.** **Doen zodra dat er is (Middleware App):** account komt al uit `multiple_accounts[0].account_id` (`Program.cs` ±132). Onthoud per account de laatste PMT-weigering met een day-cap/DLL-marker tot de eerstvolgende **18:00 ET**-grens, en toets die stand vóór `ForwardJsonAsync`. Geblokkeerd ⇒ niet forwarden, wél `GEWEIGERD` wegschrijven en melden op Discord. Zelfde plek en zelfde vorm als de kill-switch-poort (`Runtime.Armed`, regel 147). **Dit is bewust reactief:** de eerste order na een blokkade gaat nog uit en wordt door PMT geweigerd, daarna is het account dicht. De proactieve versie — echte dag-P&L per account uit de Tradovate-poller tegen de DLL van dat account — is beter maar vraagt de poller-data in de receiver; **apart item als dit draait.** _(gevraagd door Ferry 20-08; herkomst gecorrigeerd 25-08; richting bepaald en Pine-lezing rechtgezet 25-08)_ |
| **D-47** | todo | Ferry (TradingView) | **🔴🔺 ÉÉN VAN DRIE GEDAAN, EN DE RECEIVER IS INMIDDELS HERSTART — de twee resterende punten zijn nu heet.** ✅ **(2) PA015 opgelost 25-08:** één van de twee scripts verwijderd, het andere blijft actief met een nieuw aangemaakte alert. ⚠️ Die nieuwe alert is nog nooit gevuurd — controleer bij het eerste signaal dat hij het juiste `acct=` draagt. ⚠️ **Twee punten staan nog open, en de beschermende factor is weg.** Tot 01:23 UTC draaide de receiver zonder IPv4-forcering en bereikten orders PMT waarschijnlijk niet; het fantoom van 24-08 (TradingView boekte +$759,20, de broker had niets) past daarop. **Dat pad is nu gerepareerd.** Wat gisteren nergens aankwam, wordt vanaf nu een echte order. Globex is open. **(1) `REY-MNQ-P` staat op een MYM-chart** — alert `5443442753`, `CBOT_MINI:MYM1!`, met REY's MNQ-parameters `fvg=2-8`. Op MNQ is dat 0,5–2 indexpunten, op MYM 2–8; LEON draait daar niet voor niets op `fvg=12-20`. **REY vuurt daar op ruis, en die ruis wordt nu wél doorgezet.** **(3) Alert `5444047543` draagt geen account** — `acct=-0k-260824`, het lege veld, om 10:18 op 24-08. Vier minuten later kwam `5444067748` mét `PA013`. **Staat de eerste nog aan, dan vuurt PATRON dubbel.** ⚠️ **Zie ook D-52:** PATRON valt af in de vloot-sweep van 25-08. Zet die alert dus niet netjes recht — zet hem uit. _(gemeld door Pine Dev 24-08 als inbox 14; punt 2 opgelost 25-08; urgentie definitief sinds de restart van 01:23 UTC)_ |
| **D-48** | done | Middleware App + Legacy | **🔴 Alerts sneuvelen op DNS bij bursts — en de halt-close ging vandaag verloren.** Zes alerts van 24-08 kregen `Webhook delivery failed — couldn't find this domain`, op `PAT-MGC-A` (5×) en `REY-NQ-PI` (1×), terwijl alle **1.991** oudere leveringen slaagden. **Geen verkeerde URL:** alert `5444067748` leverde vandaag 3× succesvol en 5× niet met dezelfde URL, en `app.mex-traders.com` resolvet (167.233.215.60). **Het patroon is burst** — elke fout valt op een bar waar het script meerdere berichten tegelijk afvuurt (11:36 drie → 1 door, 2 weg; 11:55 drie → alle drie weg); losse berichten gingen door. **Executiegevolg:** om 11:36 faalde de PMT-`buy` en om 11:55 de PMT-`close`. Er kwam dus **geen enkele order bij PMT/Tradovate aan**. ⚠️ **Daarmee is de $759,20 die TradingView voor PATRON boekt mogelijk een fantoom** — de log bewijst alleen dat TradingView niet kon leveren. **Ferry moet in Tradovate verifiëren of er een positie open staat.** _(gemeld door Pine Dev 24-08 als inbox 13)_ **DE RECEIVER IS UITGESLOTEN — geverifieerd in de bron 24-08:** `/signal/{token}` (regel 112) kent géén concurrency- of rate-limiet. De enige twee poorten zitten **stroomafwaarts op het Discord-kaartpad**: `PostRate` (regel 561, `MEX_CARD_MAX_PER_MINUTE` default 12) en `SemaphoreSlim Gate = new(2,2)` in `CardRender` (regel 675). Drie gelijktijdige POSTs op de intake worden dus alle drie geaccepteerd. **Bovendien is de foutmelding van TradingView zelf** — *couldn't find this domain* betekent dat er geen verbinding tot stand kwam. Het probleem ligt dus **vóór** de receiver. **Nieuw spoor:** de receiver luistert op `http://localhost:5000` (uit de systemd-unit), dus er staat een reverse proxy vóór hem die TLS voor `app.mex-traders.com` afhandelt. **Die proxy is nog nooit onderzocht en is precies waar een concurrency-limiet zou bijten.** **GEVERIFIEERD 24-08 door Ferry: er staat GEEN open positie in Tradovate.** Daarmee is vastgesteld dat de buy van 11:36 nooit is aangekomen — geen weespositie, geen risico dat er iets openstaat. **Maar de trade die TradingView boekt (+$759,20 op 8 MGC) bestaat niet bij de broker.** De werkelijke P&L van 24-08 op PATRON is $0. Het bedrag zit alleen in de TradingView-strategietester en vervuilt de reconciliatie niet (die leest routed-log + Fills, en daar staat niets). **De proxy is Caddy** (`:80` en `:443`, pid 26106) — die staat vóór de receiver op `localhost:5000` en is de resterende verdachte. **GESLOTEN door Ferry 24-08: de alerts komen weer door.** ⚠️ **De oorzaak is nooit hard vastgesteld.** Caddy logde niets in het venster 11:30–12:00, maar dat bewijst weinig — Caddy schrijft doorgaans naar een bestand, niet naar journald. ⚠️ **CORRECTIE 24-08: mijn eerste verklaring (bursts) is weerlegd** door Pine Dev's eigen meting, die ik te vluchtig had gelezen — **twaalf berichten in dezelfde seconde op 18-08 werden alle twaalf geleverd**, en 24-08 was juist de rustigste dag in de log (0 fouten op 1.982 leveringen in tien dagen, daarna 6 op 15). Volume is het niet. Ook URL, domein, payloadgrootte, payloadtype en de receiver zijn met meting weggestreept. **De overgebleven hypothese is van Pine Dev en veel sterker:** alle acht alerts zijn die ochtend gemaakt, en in beide faalgevallen is het **de tweede van een paar voor hetzelfde script** — PAT-MGC-A `5444047543` (10:18, OK) vs `5444067748` (10:22, faalt), en REY-NQ-PI `5444078175` (10:24, OK) vs `5444083711` (10:25, faalt). Dat past op alerts die met **kopiëren** zijn gemaakt in plaats van opnieuw opgebouwd: het duplicaat draagt iets kapots mee dat er identiek uitziet. Voorgestelde toets: die twee verwijderen en vanaf nul opnieuw aanmaken. **Openstaande blootstelling:** er is geen alarm op leveringsfalen — vandaag verdwenen zes alerts en één trade zonder dat iets in de MEX-stack dat meldde. Zie `DECISIONS.md`. |

## 🟠 Bronnen die niet kloppen of onbereikbaar zijn

| ID | Status | Owner | Item |
|---|---|---|---|
| **D-06** | done | Ferry (VPS) | ~~De solution stond niet volledig in git~~ — **AFGEROND 25-08, commit `810728e`.** 18 bestanden: `MexJournal.sln`, `nuget.config`, de drie `.csproj` en de volledige `src/`-boom inclusief **`Mex.Journal/Recon/ReconciliationEngine.cs`** — dat was het ontbrekende stuk waar dit item om draaide. **Geverifieerd na de push:** geen secrets in de nieuwe bestanden (`mex-receiver.service`, `SETUP.md` en `Program.cs.bak` bewust weggelaten), `Caddyfile` bevat alleen de reverse-proxy naar `localhost:5000` — en bevestigt daarmee `mw.mex-traders.com` als de echte host, wat **D-49** onderbouwt. `nuget.config` heeft `<clear/>` zonder bronnen; dat is geen fout maar veilig, want **geen enkel project heeft een `PackageReference`** — de hele solution draait op het framework en bouwt dus offline. ⚠️ **Eén restpunt, gemeld aan Middleware App:** `MexJournal.sln` bevat alleen `Mex.Journal` en `Mex.Journal.Cli` — **`Mex.Journal.Receiver` staat er niet in** (0 treffers op "Receiver"). `dotnet build MexJournal.sln` bouwt de receiver dus níét. Op de VPS viel dat nooit op omdat daar op projectpad gebouwd wordt. Niet blokkerend — `dotnet build src/Mex.Journal.Receiver -c Release` werkt gewoon — maar het hoort erin. Zie **D-59**. **Dit deblokkeert D-40, D-53, D-02 en D-05.** _(was S-07 / SM-04)_ |
| **D-07** | blocked | Middleware App | **Werkelijke commissie per contract uit `Cash_History`.** De bron `backtest/config.py CONTRACTS` kent zeven waarden; Pine twee; de FLEET-validatie mat micro 0.67. Lever per contract de round-turn commissie + fees per venue. _(was inbox 1 / S-06)_ ⏸️ **GEPARKEERD door Ferry 25-08 — doen we later.** De registrywaarden ($0,52 MGC / $0,37 index-micro's) zijn voorlopig leidend; de meting uit `Cash_History` volgt wanneer het uitkomt. ⚠️ **NIEUWE BOTSING 25-08 (uit inbox 19):** alle drie de validatie-exports draaiden **commissie 0,51**, terwijl `backtest/config.py CONTRACTS` per symbool iets anders draagt — **MES staat op 0,37**. Kosten verschuiven PF direct, dus dit raakt elk cijfer uit de sweep. Volgens **D-08** wordt de Pine-commissie uit `CONTRACTS` gegenereerd; óf de generator is niet opnieuw gedraaid, óf de waarde is in TradingView met de hand overschreven. **Eén bron moet winnen** — en volgens het besluit van 24-08 is dat de registry, dus **0,37** voor MNQ/MES/MYM. 💡 **Waarschijnlijke oorzaak gevonden 25-08:** `0,51` ligt tegen MGC's `0,52` aan. Dat is hetzelfde patroon als D-08, waar een goudwaarde op de verkeerde contracten stond — alleen andersom. Wordt meegenomen in de her-export van **D-63**. |
| **D-08** | done | Pine Dev | **De commissie in Pine is een kopie van een contract-spec.** Ook als het getal klopt is een handmatig getal een tweede bron. `PropFirms.pine` wordt al gegenereerd; de contract-specs niet. Onafhankelijk van D-07 op te lossen. _(was SM-06)_ **GOEDGEKEURD door Ferry** (afgevinkt in LifeOS, geconstateerd 24-08). Volgt automatisch D-07 zodra de commissiebron wijzigt — dan generator herdraaien. |
| **D-09** | done | Backtest Setup | **CVD-tegenspraak — OPGELOST 24-08 door het fleet-pakket van 23-08.** De canonieke onderzoeks-CVD is een deterministische **OHLCV-polariteitsproxy**, niet de native Delta-kolom en niet `ta.requestVolumeDelta()`. Daarmee is `Delta ≡ 0` in `ES/GC/YM_norm.csv` géén tegenspraak: de proxy wordt uit OHLC berekend, die kolom doet niet mee. Het playbook beschreef dus geen andere strategie. Native Delta blijft een los experiment dat de proxy nooit stilzwijgend mag vervangen. _(was S-09 / SM-05)_ |
| **D-10** | todo | Backtest Setup | **CVD-diepte van de NQ-dataset is onbekend.** Blokkeert optie A van D-18. Wacht op validator-output; D-36 (NQ pilot-export) is opgeleverd ✅. _(was S-13 / SM-09)_ |
| **D-11** | review | Middleware App + Ferry | **Secrets roteren — ⚠️ TWEE MET VOORRANG (20-08):** `MEX_WEBHOOK_SECRET` en `MEX_DISCORD_WEBHOOK` zijn op 20-08 in platte tekst door een chattranscript gegaan (`systemctl cat mex-receiver`-uitvoer). Roteer die twee eerst; de overige vier volgen de gewone lijst. — checklist en rotatie-instructies staan nu bovenaan `middleware/docs/SECRETS-REGISTER.md`. Zes secrets (`NOTION_TOKEN`, `NOTIFY_WEBHOOK`, Fase C endpoint-token, `VIEWER_PASSWORD`, `MEX_WEBHOOK_SECRET`, `VIEWER_API_TOKEN`) zijn in chats/transcripten langsgekomen sinds 9-aug — per secret staat er nu *waar roteren, waar opslaan, welke service herstart, verificatie-commando*. **Resteert voor Ferry:** de zes secrets daadwerkelijk roteren en de ☐-vakjes afvinken. _(was SM-07)_ |
| **D-20** | wip | Scrum Master | **De Notion-ids in `CLAUDE.md` deels dood — deels opgelost 25-08.** *MEX Reconciliation* is **niet meer dood**: 334 rijen, geverifieerd 24-08. Resteert: Fleet Performance en Trade Journal controleren, en de Content Hub-datasource. Pak ik zelf op. |
| **D-55** | todo | Backtest Setup | **De vloot-sweep heeft geen spoor in `validation/`.** De conclusies staan in `docs/DECISIONS.md` en in `pipeline_state.json` — en dat laatste staat in de lab-map, niet in git. **`validation/` is de append-only bewijsmap** (eigenaarstabel D-19) en juist daarom bruikbaar gebleven toen de GC+ES-conclusie eronder wegviel: het bewijs bleef staan met een intrekkingsnotitie erbij. Een vloot-rangorde zonder bestand daar is over een maand niet meer te auditen — en gezien D-54 is de kans reëel dat deze cijfers herzien worden. **Doen:** een `FLEET_sweep_20260825`-bestand in `validation/` met per engine de trap-uitkomsten, de poortstatus, het meetvenster (`--since 2023-08-24`) en de twee groottes naast elkaar. Zelfde vorm als `FLEET_stage3-10_verdicts_20260810.md`. _(uitgezet door Scrum Master 25-08)_ |
| **D-56** | todo | Backtest Setup | **Echte MGC-data ontbreekt — élk MGC-oordeel staat onder voorbehoud, ook een afwijzing.** PATRON, TESORO en BANDIDO zijn gemeten op de **GC-twin**. De sweep concludeert dat PATRON geen edge heeft (−$2,76/trade, PF 0,96, trap 9 expliciet CHERRY-PICK) en dat TESORO niet fundeert op 1 contract — maar beide op een instrument dat ze niet handelen. **PATRON is daarom NIET dood verklaard.** De twijfel is zwaar en de trap-9-uitkomst sluit aan bij wat al weerlegd was (fine-grained uur/dag-maskers zijn OOS-ruis), dus zet er geen nieuw werk in — maar een definitieve afwijzing vraagt echte MGC-data. ⚠️ **Dit raakt D-44 en D-47:** PATRON zit in de negen scripts van D-44, en de accountloze PATRON-alert uit D-47 moet hoe dan ook uit — dat is alerthygiëne, geen vlootbesluit, en staat los van dit item. **Doen:** vaststellen of MGC-1-minuutdata te krijgen is en tegen welke moeite; zo niet, dan expliciet vastleggen dat de MGC-bucket op de GC-twin beoordeeld blijft en wat dat aan zekerheid kost. _(uitgezet door Scrum Master 25-08 op de sweep-bevinding)_ |
| **D-58** | todo | Ferry (GitHub) | **De default branch van de repo wijst naar een dode branch.** Opgemerkt 25-08 op de repo-instellingenpagina: default staat op `claude/mcp-trader-dev-sse-ibl64y` — en die is in `CLAUDE.md` al als dood gemarkeerd, volledig opgenomen in de werkbranch en 186 commits achter. **Gevolg:** wie de repo opent ziet standaard maanden oude code — een nieuwe Claude-sessie die de default kloont, de webinterface, een PR die zijn basis pakt. Dat is precies het soort stille veroudering waar dit project al twee keer op is vastgelopen. **Doen:** GitHub → repo **Settings** → *General* → **Default branch** → ⇄ → `claude/middleware-setup-guide-afhvtk`. Twee klikken en omkeerbaar. _(opgemerkt door Scrum Master 25-08)_ 📏 **GEMETEN 25-08, en het maakt de keuze makkelijker:** `main` staat nog op `c767a0d` — **letterlijk "Initial commit" van 29-07**. De werkbranch loopt **444 commits vóór** en `main` draagt **niets** wat er niet in zit. Een merge naar `main` is dus een schone fast-forward zonder conflicten en zonder verlies; er valt niets af te wegen. **Mijn advies blijft toch: zet de default nú op de werkbranch.** Twee klikken, altijd actueel, en het lost het probleem vandaag op. `main` fast-forwarden kan wanneer je wilt — maar een `main` die je daarna niet bijhoudt geeft over een maand hetzelfde probleem, alleen met een nettere naam. ⚠️ **Wat Ferry hierbij noemde** — *"alles wat vandaag live ging staat op een branch die nog niet gemerged is"* — klopt ten opzichte van `main`, maar niet ten opzichte van **D-13**: die merge is 24-08 uitgevoerd en door hem goedgekeurd. `Program.cs` mét `97c50cd` staat sindsdien in de werkbranch, en dat is precies wat D-06 en D-35 mogelijk maakte. Het losse eind is `main`, niet de receiver-code. |
| **D-59** | todo | Middleware App | **`Mex.Journal.Receiver` ontbreekt in `MexJournal.sln`.** Vastgesteld 25-08 direct na D-06: de solution kent alleen `Mex.Journal` en `Mex.Journal.Cli`; op "Receiver" geeft het bestand **0 treffers**. Wie `dotnet build MexJournal.sln` draait bouwt het live executiepad dus niet mee — en merkt dat niet, want de build slaagt. Op de VPS viel het nooit op omdat daar op projectpad gebouwd wordt (`dotnet build src/Mex.Journal.Receiver -c Release`). **Niet blokkerend, wel een valkuil**: een CI-stap of een nieuwe ontwikkelaar die de solution bouwt krijgt groen zonder de receiver te hebben aangeraakt. **Doen:** `dotnet sln middleware/dotnet-receiver/MexJournal.sln add middleware/dotnet-receiver/src/Mex.Journal.Receiver/Mex.Journal.Receiver.csproj`. Eén commando, en dan bouwt de solution wat hij hoort te bouwen. _(opgemerkt door Scrum Master 25-08 bij de verificatie van D-06)_ |
| **D-61** | review | Pine Dev | **`MEX_EL_BANDIDO_MYM_HF_EOD_v1_0_0.pine` compileert niet — één regel.** Regel 236: `dayExitMode` heeft defval `"Cap only"`, en die staat **niet in de eigen `options`-lijst** (`Off` / `Day-trail (keep peak)` / `Day-cap (hard target)` / `Trail + cap`). Pine v6 weigert een `input.string` waarvan de default buiten `options` valt. De andere acht scripts hebben allemaal een geldige default. ➡️ **Dit verklaart waarschijnlijk waarom BANDIDO in het pakket-README als "pariteit open, niet live" staat: het is geen openstaande meting maar een compileerfout.** Backtest Setup heeft `pine/**` niet aangeraakt (jullie map) en mapt het in hun mirror op `"Day-cap (hard target)"` — maar dat is *een interpretatie*, dus het staat in `fleet.PINE_DEFECTS` en BANDIDO blijft daardoor open op trap 1. **Corrigeer je de bron, dan halen zij de uitzondering weg.** 💡 Meegeleverd: `backtest/tests/test_fleet_parity_source.py` leest `pine/v1_0_0/*.pine` bij elke testrun en faalt bij afwijking, plus een test die élke `input.string` met een ongeldige default opspoort. Wijzig je een script, dan meldt hun suite meteen dat de mirror achterloopt. _(gemeld door Backtest Setup 24-08 als inbox 18; genummerd 25-08)_ **HERSTELD 25-08 (Pine Dev).** `dayExitMode` staat nu op `"Day-cap (hard target)"` — dezelfde waarde die jullie mirror al aannam, dus de uitzondering in `fleet.PINE_DEFECTS` kan weg. Bevestigd door de bevroren spec zelf: BANDIDO draagt een **harde dagcap van $1.000** en `dayCapUSD` staat ook op 1000, dus "Cap only" was een tikfout voor de cap-modus, geen trail. Versie naar v1.0.2. Ik heb dezelfde controle over alle veertien scripts gedraaid — dit was de enige ongeldige `input.string`-default in de hele vloot. |
| **D-64** | todo | Pine Dev | **`MEX_EL_DORADO.pine` compileert niet — en dit keer is de generator de bron, dus handmatig herstellen helpt niet.** Gevonden 25-08 bij een eigen sweep over alle 22 `.pine`-bestanden, direct na D-61. `MEX_EL_DORADO.pine` regel 221: `firmPreset` heeft defval **`"apex_intraday_pa"`**, en die sleutel **bestaat niet in de registry** — de juiste is `apex_50k_intraday_pa`. Pine v6 weigert een `input.string` waarvan de default buiten `options` valt, dus dit is exact dezelfde fout die Pine Dev vanochtend in BANDIDO ophaalde. ⚠️ **Het verschil met D-61: hier zit de fout in `tools/gen_pine_firms.py`.** In `STRATEGY_DEFAULT` (regel 103) staat `"MEX_EL_DORADO.pine": "apex_intraday_pa"`, en regel 226-228 schrijft die waarde letterlijk als default terwijl de `options` uit de registry komen. **Een handmatige correctie in het `.pine`-bestand wordt bij de volgende generatorrun overschreven.** Fix hoort in de map, niet in het script. 💡 **Nuance bij de melding van vanochtend:** Pine Dev schreef *"dezelfde controle over alle veertien scripts gedraaid: dit was de enige ongeldige `input.string`-default in de vloot."* Dat klopt **voor de v1_0_0-vloot** — mijn sweep bevestigt dat daar geen tweede in zit. DORADO valt buiten die negen, dus het is geen tegenspraak, wel een gat in de scope van de controle. **Draai die check over `pine/**`, niet over `pine/v1_0_0/**`.** _(gevonden door Scrum Master 25-08; `tools/gen_pine_firms.py` is per de eigenaarstabel expliciet van Pine Dev, dus alleen gemeld)_ |
| **D-65** | todo | Pine Dev | **De firm-preset-generator kent de v1_0_0-vloot helemaal niet — de negen live scripts staan buiten zijn kaart.** `tools/gen_pine_firms.py` `STRATEGY_DEFAULT` bevat alleen de oude v6.9.5-bestandsnamen (`MEX_EL_TESORO.pine`, `MEX_EL_REY.pine`, …). De negen `v1_0_0`-scripts komen er niet in voor. De generator zegt het zelf in een comment op regel 96-98: *"Deze generator kent de v1_0_0-lijn nog niet — zie `docs/inbox.md` item 18 voor wat dat kost."* **Gevolg:** de firm-dropdown en het firm-preset van de live vloot worden met de hand onderhouden, terwijl D-08 juist vastlegde dat dit gegenereerd hoort te worden omdat een handmatig getal een tweede bron is. 🔍 **HYPOTHESE, expliciet als hypothese — dit kan de wortel onder D-63 zijn.** LEON is gevalideerd onder `apex_50k_intraday_pa` terwijl de bron `apex_50k_eod_pa` draagt. Backtest Setup schreef dat toe aan TradingView dat oudere inputs bewaart, en dat kan kloppen. **Maar een handmatig onderhouden firm-preset is een minstens zo goede kandidaat** — en die zou het ook opnieuw laten gebeuren na de her-export van D-63. **Pine Dev: kijk hiernaar vóór je D-63 uitvoert.** Is dit de oorzaak, dan is de her-export dweilen met de kraan open. **Doen:** de negen `v1_0_0`-bestanden in `STRATEGY_DEFAULT` opnemen met hun bedoelde firm-programma, en de generator over de vloot laten lopen. _(gevonden door Scrum Master 25-08 bij het natrekken van D-64; hangt samen met D-08, D-63 en inbox 18)_ |
| **D-62** | done | Scrum Master | ⛔ ~~MATADOR drawdown-model EOD vs Intraday~~ — **INGETROKKEN 25-08, zelfde fout als D-60.** Backtest Setup had dit in inbox 19 al zelf teruggenomen, met zoveel woorden: *"Het MATADOR EOD/Intraday-conflict bestaat niet. (…) Haal dit uit de Approval Queue — er valt niets te beslissen."* **Reden:** alle negen scripts draaien met `Use firm preset = On`, dus `f_firmRules` overschrijft de losse `Drawdown Model`-input. MATADOR's `apex_50k_eod_pa` levert EOD; de default "Intraday" wordt nooit gebruikt. De fout zat in hun eigen mirror en is opgelost door het model uit het firm-programma af te leiden. **Ik heb een ingetrokken item als taak uitgegeven.** Het stond alleen op het bord en niet in Notion, dus daar viel niets op te ruimen — D-60 wél, die is daar als ingetrokken gemarkeerd. _(uitgegeven en dezelfde dag ingetrokken door Scrum Master)_ |
| **D-49** | todo | Middleware App | **Alles moet naar `mw.mex-traders.com` verwijzen** (besluit Ferry 25-08). `middleware.pipsandpalmtrees.com` geeft NXDOMAIN en **dat domein wordt niet actief gebruikt** — het is dus geen DNS-taak maar een opruimtaak. **Doen:** elke verwijzing in de repo naar die host vervangen door `https://mw.mex-traders.com/signal/<token>`, te beginnen bij `middleware/docs/SETUP.md` regel 11. Controleer daarna dat README.md en SETUP.md dezelfde URL noemen — nu spreken ze elkaar tegen. _(gemeld door Pine Dev 24-08 als inbox 15)_ |
| **D-50** | todo | Backtest Setup | **EL TORO: twee gaten tussen de scripts en de frontier.** _(gemeld door Pine Dev 24-08 als inbox 11 — details in `docs/inbox.md`)_ |

## 🔵 Werk dat vastzit op een merge

| ID | Status | Owner | Item |
|---|---|---|---|
| **D-12** | done | Backtest Setup | **`validation/` bewijs staat alleen op de legacy-branch** — stage 1-2 preregistraties én 3-10 verdicts, het onderliggende bewijs voor *GC + ES funded edge, NQ/YM eval-only*. _(was SM-08)_ **OPGELOST 24-08 door de legacy-merge** (`e2dd7df`): `validation/` staat in de werkbranch, 20 bestanden. ⚠️ Het bewijs onderbouwt de **ingetrokken** GC+ES-conclusie — historie, geen geldende waarheid. |
| **D-43** | todo | Backtest Setup | **De analyses-branch botst architectonisch met de werkbranch in `backtest/**`.** Merge afgebroken 24-08 omdat dit geen samenvoegbare botsing is maar twee parallelle ontwerpen van hetzelfde model — en een verkeerde keuze breekt stil iets. **Drie botspunten:** (1) `engine.py` — de werkbranch heeft `resolve_bar`, analyses heeft `halt_bar`; twee namen voor hetzelfde begrip, en `funnel.py` van elke kant gebruikt de eigen variant. (2) `funnel.py` — `FunnelOutcome` draagt in de werkbranch `days` (handelsdagen tot resolve), in analyses `resolve_sessions` plus `payouts`/`banked`/`breaches`/`milks`. (3) `run.py` regel ~326 — de werkbranch roept `ind_mod.compute(dtf, cfg)` met het **datumgefilterde** frame, analyses roept `compute(df, cfg)` met het ongefilterde; neem je die van analyses over, dan werkt `--since`/`--until`/`--window` stil niet meer op de funnel. Analyses brengt daarnaast twee skills mee (`eval-throughput`, `payout-throughput`) en `goals.py`. **Backtest Setup beslist**, niet de Scrum Master — dit is een open ontwerpkeuze in jullie eigen map. _(uitgegeven 24-08 bij de merge-ronde)_ **ADVIES Scrum Master 25-08:** neem **de werkbranch als basis** en breng alléén analyses' nieuwe *toevoegingen* over — `goals.py`, de twee skills (`eval-throughput`, `payout-throughput`) en de engine-fixes — maar **niet** hun hernoemingen. Reden: de werkbranch draagt `resolve_bar`, `days` en de datumgefilterde aanroep, en die drie hangen samen met `--since`/`--until`/`--window`, met de funnel en met wat er nu al draait. Analyses' `halt_bar` en `resolve_sessions` zijn betere namen maar geen betere functionaliteit, en de winst weegt niet op tegen het risico dat `--since` stil stopt te werken. **Concreet: cherry-pick de nieuwe bestanden, laat `engine.py`/`funnel.py`/`run.py` van de werkbranch staan, en hernoem later als iemand er tijd voor heeft.** Backtest Setup mag dit overrulen — het is hun map. |
| **D-46b** | review | Middleware App (gebouwd door Pine Dev) | **Executiepoort: geen geaccepteerde PMT-order, geen trade** (besluit Ferry 24-08). Een Discord-kaart wordt alleen een trade bij een `pmt`-regel voor hetzelfde account+symbool+richting, kort vóór de fill, met `result` = `sent 200`. Vervangt de oude `amap`-check (*'dit account stuurde ergens in het venster een payload'*), die op 24-08 PATRON's trade doorliet terwijl de order nooit vertrok. Whitelist niet blacklist, fail-closed zonder orderlijst, en elke geweigerde fill komt als `GEEN EXECUTIE` in de log plus `unconfirmed` in de samenvatting — wat afvalt mag niet stil afvallen. Poort zit op de **entry**: een TP/SL-exit levert nooit een PMT-close (PMT houdt de bracket server-side), dus een exit-poort had bijna elke trade geweigerd. **Let op het label:** `sent 200` is de status van ónze POST, niet PMT's oordeel — dit journaal bevat *aangenomen orders*, geen bevestigde fills. Ook `dashboard_state.py` en `viewer.py` aangepast, anders waren die stil leeg geworden. 10 nieuwe tests, 155 passed. Vervolg: receiver PMT's responsebody laten meeschrijven (raakt .NET, dus D-06/D-35-build). Uitgewerkt in `docs/inbox.md` item 17. |
| **D-44** | todo | Pine Dev | **🔺 De BE-offset bereikt de broker nooit — OORZAAK BEWEZEN 25-08, fix is één veld.** ⚠️ **Dit weerlegt de eerdere conclusie in dit item.** Legacy schreef 20-08 dat `breakeven_offset` geen JSON-veld is maar een dashboard-instelling, op grond van PMT's feature-pagina. **Onjuist.** Ferry leverde 25-08 een alert-template die hij ín PMT heeft aangemaakt, en daar staat het veld gewoon in: `"breakeven": 5, "breakeven_offset": 2`. Het veld bestaat en PMT verwacht het. **Geverifieerd in alle negen `v1_0_0`-scripts:** `f_pmtJSON` draagt `breakeven` wél en `breakeven_offset` **niet** (0 treffers, negen van de negen), terwijl `beOffEff` overal bestaat (regel 207, input `beOffsetSize` default 12) en intern al gebruikt wordt om de stop te verzetten (regel 1557/1580). De offset wordt dus correct berekend en alleen niet meegestuurd. **DE FIX — in `f_pmtJSON`, direct achter het `breakeven`-veld, zelfde guard en zelfde transform:** `+ ",\"breakeven_offset\":" + ((useBEeff and _slD > 0) ? str.tostring(f_distPrice(beOffEff)) : "0")` — negen bestanden, één regel per stuk. **Drie dingen erbij:** (1) `f_distPrice()` rekent om vanuit `unitMode`, precies zoals bij `breakeven`, dus de eenheid klopt automatisch mee; (2) Ferry's template bevestigt ook dat `breakeven` een **afstand** is en geen prijs — wat we sturen was al goed; (3) drie andere velden uit de template sturen we niet (`strategy_name`, `stp_limit_stp_price`, `same_direction_ignore`) maar die staan er alle drie op hun neutrale waarde, dus die kunnen weg blijven. ⚠️ **Blijft staan uit het eerdere onderzoek:** staat het risk type van dat symbool/account in PMT op **`Price`**, dan past PMT breakeven én offset helemaal niet toe — en wij sturen `dollar_sl`. **Controleer dat vóór je de fix test**, anders test je in het donker. **Raakt echte orders — melden vóór pushen.** ✅ **BLIJFT IN PINE — beslist 25-08 na Ferry's vraag of de middleware dit kon injecteren.** Drie redenen: (1) de waarde is een **bevroren strategieparameter** per engine (`beOffsetSize`, zie `frozen-engines.md`); injecteren in de receiver vraagt een tweede tabel strategie→offset naast de Pine-input, en dat is de klassieke driftbron — iemand wijzigt de input, de middleware stuurt de oude waarde. (2) Pine gebruikt `beOffEff` **zelf al** om zijn eigen stop te verzetten (regel 1557/1580); lopen die twee uiteen, dan verzet de simulatie de stop naar entry±12 terwijl PMT hem elders legt — precies de pariteitsklasse fouten die deze pijplijn al een hele rangorde heeft gekost. (3) `f_distPrice()` rekent om met de tick-math van het instrument; in de receiver zou dat herbouwd moeten worden in C#, dus een tweede implementatie van dezelfde formule. **En het handwerk valt mee:** de `f_pmtJSON`-body is **byte-identiek in alle negen scripts** (md5 `9812c09d5b` over het blok, negen van de negen). Eén `sed` op dat anker doet ze allemaal en een checksum achteraf bewijst het — geen negen handmatige edits. ➡️ De onderliggende zorg van Ferry is wél terecht en staat nu apart als **D-51** (die byte-identieke blok negen keer kopiëren ís schuld). _(gemeld door Legacy 20-08; oorzaak bewezen met Ferry's PMT-template 25-08)_ |
| **D-13** | done | Scrum Master | **Merge-plan drie branches — 24-08 uitgevoerd voor zover veilig.** ✅ **legacy gemerged** (`e2dd7df`, 33 commits, 59 bestanden): `middleware/dotnet-receiver/Program.cs` mét `97c50cd` en de `validation/`-map staan nu in de werkbranch — dat deblokkeert D-06 en levert de bron voor D-35. ✅ **pine-dev** was al volledig opgenomen (0 commits vooruit). ❌ **analyses NIET gemerged** — echte architectuurbotsing in `backtest/**`, zie D-43. ❌ **discord-notify NIET gemerged** — `notices.py` is dood sinds D-04 en het waardevolle deel zit al in de .NET-receiver; de tests staan nog op de branch als iemand ze wil oogsten. _(was S-03)_ **GOEDGEKEURD door Ferry 24-08** — afhandeling blijft zoals uitgevoerd: legacy binnen, analyses via D-43 bij Backtest Setup, discord-notify blijft liggen. |
| **D-36** | done | Ferry | **Lever de NQ pilot-export met CVD-diepte** — deblokkeert D-10 en daarmee optie A van D-18. Formaat volgens `docs/data_export.md` (Quantower-spec, analyses-branch). **OPGELEVERD 20-08.** Backtest Setup draait `tools/validate_dataset.py` met de CVD-gate en rapporteert op het bord (D-10). _(gevraagd door Backtest Setup 19-08)_ |
| **D-37** | done | Legacy (of Ferry) | **Lever één recente live alert-payload** uit het executiepad — deblokkeert D-09. Backtest Setup vergelijkt hem tegen het playbook en de norm-datasets om te bepalen of de live scripts CVD aan of uit hebben. **OPGELEVERD 20-08.** Bijkomende bevinding: als PMT-embed aangeeft dat een account geblokkeerd is (day cap/DLL), mag de receiver het signaal niet forwarden → zie D-40. _(gevraagd door Backtest Setup 19-08)_ |
| **D-19** | done | Ferry | ~~Drie mappen zonder eigenaar~~ — **AKKOORD Ferry 25-08.** `docs/**` → Scrum Master; `tools/**` en `validation/**` → Backtest Setup, met `tools/gen_pine_firms.py` als uitzondering bij Pine Dev en **`validation/` append-only**. De eigenaarstabel in `CLAUDE.md` is compleet gemaakt en draagt nu ook `web/**` → Web (D-29). _(was S-02)_ |

## 🟡 Lopend werk

| ID | Status | Owner | Item |
|---|---|---|---|
| **D-14** | done | Backtest Setup | `backtest/funded.py:19` heeft `APEX_DD` hardcoded terwijl `firms.py` de registry al leest. **OPGELOST** `636c4eb`: `funded.py` leest via `funded.apex_rules(size)` registry-first; fallback naar oude constanten voor formaten die ontbreken. Behavior-delta gedocumenteerd: consistency 30%→50%, qualifying days 8→5, DLL $4.000 wordt nu afgedwongen. _(was inbox 2)_ |
| **D-15** | todo | Backtest Setup | ATR-kalibratie MR·FVG: tick-tuning omrekenen naar ATR(14)-veelvouden en sweepen op MGC + ES + NQ. _(was inbox 3)_ |
| **D-16** | todo | Backtest Setup | `calc_on_order_fills=true` — wordt dit de norm voor de funnel? Trefkans 81% → 74,6%, PF 1,15 → 0,93. Eerlijker model, maar het verandert welke trades vuren. _(was inbox 5)_ |
| **D-17** | review | Middleware App | Viewer-rol (units-only) overnemen uit `web/handover/mex_units/` + `public-stats.json` periodiek schrijven. **OPGELEVERD:** `middleware/app/mex_units/` (overgenomen, 17 tests), `middleware/app/public_stats.py` (adapter + writer, atomic swap, 4 tests), `/public-stats.json` endpoint in de viewer (no-auth, CORS `*`, `max-age=300`), en `deploy/mex-public-stats.{service,timer}` (elk uur). `assert_no_currency()` is de publicatiepoort — geen bedrag verlaat het pand. **Resteert voor Ferry:** `sudo cp deploy/mex-public-stats.{service,timer} /etc/systemd/system/ && sudo systemctl daemon-reload && sudo systemctl enable --now mex-public-stats.timer` + viewer herstarten voor het endpoint. _(was Web-inbox 4)_ |
| **D-18** | blocked | Ferry | **OOS-venster 2023-2026 is opgebrand** — herselecteren op pre-2023 of heretiketteren als validatie. Blokkeert elke publieke claim. Blocked by D-10 voor optie A. |
| **D-21** | done | Ferry | ~~Account 214 stond nog als eval~~ — **AFGEROND door Ferry 25-08.** Het account is Intraday; EL PATRON op 8 MGC was daarmee uitgesloten. _(was S-12)_ |
| **D-34** | review | Web | **Publieke claims in `web/**` afzwakken zolang D-18 open staat.** De sites beweren op zes plekken dat er een *gevalideerde edge* is; het OOS-venster daarachter is opgebrand. Betreft `public-stats.json`, `resultaten.astro`, de homepage, de pijlerpagina, `methodiek.mdx` en de prop-firm-gids. Begrippen die het *concept* validatie uitleggen blijven staan. Blocked-by-strekking van D-18, maar zelfstandig uit te voeren. |
| **D-38** | todo | Backtest Setup | **Portefeuille-selectie: decorrelatie meten, niet aannemen.** De mill beoordeelt kandidaten op PF per stuk — dat vindt dezelfde edge N keer terug. Aanpak: dagelijkse P&L-reeks per OOS-overlever → correlatiematrix + gedeelde-verliesdagen + regime-complementariteit → hebzuchtige selectie die een **set** oplevert, niet een ranglijst. Grondstof (`trades.csv` per run) ligt er al. Ferry heeft bouw goedgekeurd 19-08. Volledig binnen `backtest/**`. _(ingediend door Backtest Setup 19-08 als inbox 6)_ |
| **D-39** | todo | Backtest Setup | **Eval-lens: spectrum-zoektocht over prop-firm programma's.** Welke combinatie haalt *welk* prop-firm-account het snelst? De machinerie is er grotendeels (`--firm`/`--funnel`, 13 eval-programma's in de registry). Ontbreekt: sweep over programma's met pass-rate + *tijd-tot-pass in dagen*. **Aandachtspunt:** Ferry's bereik gaat tot 4M, de registry stopt bij 250k — ontbrekende programma's toevoegen raakt `data/propfirms.json` (gedeelde bron, §3). **Verzoek aan Ferry:** besluit wie de 300k–4M programma's toevoegt en welke bron daarvoor gezaghebbend is. Backtest Setup bouwt de sweep daarna. _(ingediend door Backtest Setup 19-08 als inbox 7)_ |
| **D-41** | todo | Pine Dev | **Elf guard-kaarten dragen geen account, dus de per-kanaal routing kan ze niet splitsen.** Bij het bouwen van D-28/2 stelde Legacy vast dat de helft van de emitters geen account in de payload zet: **DAY HALT · LIMIT EXPIRED · AUTO FLAT · ACCOUNT HALT · SIGNAL BLOCKED · CONFIG · PAYOUT · CAP LOCK · PASSED · FAILED · PA THRESHOLD**. Waar het account zou staan, staat bij DAY HALT de haltReden en bij LIMIT EXPIRED een prijs. Gevolg: juist de account-niveau veiligheidskaarten landen op de globale `NOTIFY_WEBHOOK` en zijn niet naar een funded- of eval-kanaal te sturen — een halt op funded ziet er hetzelfde uit als een halt op eval. **Verzoek:** `jrnlAcct + " | "` vooraan in de description van die elf emitters (of de eval-tail eraan hangen); één regel per emitter. **Let op:** de receiver herkent alleen de vorm `PA016-0k-260813` (`^[A-Z]{2}\d{3}-`), dezelfde die `render-signal.js` gebruikt — wijkt `jrnlAcct` qua vorm af, dan valt de routing stil terug op globaal in plaats van te falen. Maakt D-28/2 pas volledig werkend. _(ingediend door Legacy 20-08 als inbox 8)_ **HOE (Scrum Master 25-08):** Pine Dev-werk, elf regels. In elk van de elf emitters gaat `jrnlAcct + " | "` vooraan de `description` van de `f_sendDiscord`-aanroep — dezelfde vorm die FILL en EXIT al gebruiken. ⚠️ **Eén valkuil:** de receiver herkent alleen het patroon `^[A-Z]{2}\d{3}-` (zoals `PA016-0k-260813`), hetzelfde patroon dat `render-signal.js` gebruikt. Wijkt `jrnlAcct` daarvan af, dan valt de routing stil terug op de globale webhook in plaats van te falen — dus je merkt het niet. **Laat Pine Dev na de wijziging één guard-kaart afvuren en controleren dat hij op het juiste kanaal landt**, niet op het globale. Dit maakt D-28/2 pas volledig werkend. |
| **D-42** | todo | Pine Dev | **De `v1_0_0`-vloot vervangt `pine/**` — BESLIST door Ferry 24-08.** De negen scripts staan in `pine/v1_0_0/`, het pakket-README en de onderzoeksnotities in `docs/fleet-2026-08-23/`. **TESORO-conflict beslecht: het pakket wint.** `TES-MGC-C` (7 MGC, FVG 11–16, CVD6, SL 140t, 2,25R) wordt de TESORO; **v7.9.5 gaat naar `.bak`**, niet weggegooid. **D-45 en D-46 zijn daarmee expliciet ingetrokken** — zie `DECISIONS.md`; dat was de voorwaarde bij deze keuze, geen stilzwijgende overschrijving. **Defect 1** (dubbele merkkop EL MATADOR / EL CENTINELA) is beslecht. **Defect 2** (validatie-exports dragen niet de naam van hun script) is vastgelegd — leg de koppeling vast vóór een export als bewijs geldt. **EL BANDIDO gaat niet live**, Pine-pariteit staat open. _(uitgegeven 24-08; TESORO beslist door Ferry 24-08)_ **HOE (Scrum Master 25-08):** het is Pine Dev's werk, jij hoeft niets te doen. Volgorde: (1) `cp pine/MEX_EL_TESORO.pine pine/MEX_EL_TESORO.v7.9.5.bak` — de back-up die je bij optie 2 hebt bedongen; (2) de negen bestanden uit `pine/v1_0_0/` naar `pine/` verplaatsen en de acht oude verwijderen; (3) `pine/VALIDATION_MAP.md` bijwerken zodat elk script naar zijn eigen export wijst — dat is defect 2, en zonder die koppeling is geen enkel cijfer navolgbaar; (4) `CHANGELOG.md` een regel geven die de vervanging vastlegt inclusief de intrekking van D-45/D-46. **Jij komt pas weer in beeld bij de alerts:** de nieuwe scripts moeten in TradingView op de juiste charts met de juiste accounts — en dat is precies waar D-47 vandaag misging. Doe die twee in één ronde. |

## ⚪ Later

| ID | Status | Owner | Item |
|---|---|---|---|
| **D-22** | done | Middleware App | ~~Chart-snapshot stond 3× open~~ — **BESLIST door Ferry 25-08: alle drie verwijderen.** De renderer-haak (`chartUrl`) blijft bestaan; als de feature ooit terugkomt is het een nieuw item. "Kan altijd nog." _(was SM-10)_ |
| **D-23** | blocked | Middleware App | ⏸️ **NAAR TO BE REFINED door Ferry 25-08.** Sharpe uit de compliance-monitor is nooit gebouwd. Niet vervallen, wel bevroren tot iemand hem opnieuw scopet — Sharpe zegt weinig over gebankte payout per account-dag, en dat is inmiddels het doel. _(was SM-11)_ |
| **D-45** | done | Pine Dev | **Daily risk-gate in TESORO v7.9.3** (uit `docs/pine_dev_prompt_v7.9.2.md`, branch `analyses-data-chat-org`). DLL + trail op GEREALISEERDE sessie-P&L, trail alleen op trade-close, venster 18-23 ET, reset op CME-roll. Voedt `dayHalted`, dus cancel/close_all/`f_sendExec("close")`/entry-blokkade komen uit de bestaande machinerie — geen tweede executiepad, geen nieuwe alert-payload. Firm-regels blijven uit de registry (ladder, dagentellers). Defaults uit de export e5951. **v7.9.4: DLL-basis omgezet naar verliesbegrenzer** (eigenaar 20-08). Nagerekend op e5951 venster 18-23 T10/B100/DLL150: begrenzer q3 **+$5.067** / q6 **+$26.684** tegen halt-trigger q3 -$9.851. Aantal halts identiek (116 DLL / 24 trail / 111 natural) — het verschil is uitsluitend de kosten van de trade die de dag sluit. Wacht op compile + validatierun. **v7.9.5: DD-model/trailing DD/PA-DLL volgen nu de registry** — `f_firmRules` leverde `_fkDD/_fkMax/_fkDLL` al maar niemand gebruikte ze, dus een intraday-programma werd als EOD gerekend. Zelfde vorm als D-08. ⚠️ **INGETROKKEN 24-08** — Ferry koos bij D-42 voor het pakket boven v7.9.5. Deze daily risk-gate zat in de v7.9.5-lijn en gaat mee naar `.bak`. Het werk was niet fout; het is bewust ingeruild. Terughalen kan uit de `.bak` als de keuze ooit herzien wordt. |
| **D-46** | done | Pine Dev | **Exit-engine test: de trail uit — geslaagd, en de eerste payouts ooit in de simulatie.** Vijf exports 20-08 (trail EN break-even uit, DLL-basis *realized only*, dagcap $750 hard, venster 18-23). q3: 341 trades, win% 44,6, **gem. winst $424 tegen $61 met trail aan**, gem. verlies $301, PF 1,130, net +$7.399, maxDD c2c $4.860. q6: 252 trades, PF 1,063, net +$5.334, maxDD $11.499 — de dagcap van $750 knipt bij dubbele size 76 van de 252 trades af (30%) tegen 40 van de 341 (12%) bij q3, dus **q6 verdient minder dan q3 en breacht op dag 10**. Chronologisch met de echte regels: q3 haalt 6 payouts / $5.542 (eod_pa) resp. 5 / $7.331 (legacy_pa). **Diagnose van de breach: niet de strategie maar het opnamebeleid.** Zonder opnames haalt dezelfde q3-curve alle 249 sessies zonder breach; met maximale opname staat de balans na elke payout weer op $52.600 terwijl de vloer op $50.100 vaststaat — $2.500 kussen tegen een maxDD van $4.860. Met een opnamebuffer (houd de balans na opname boven vloer + $4.000, dus >= $54.100) haalt eod_pa **6 payouts / $6.161 zonder breach**: meer geld eruit en het account blijft leven. Plateau ligt breed ($3.500-$6.000 werkt), dus geen curve-fit. Drie van de vijf exports (9e1d6 EOD/EOD, d22c0 Intraday-input/EOD-registry, f50d0 Intraday/Intraday) leveren **byte-identieke traderijen** — bevestigt dat het DD-model in `paBacktestMode` alleen markeert en niet handelt, en dat v7.9.5 de registry laat winnen van de losse input. Volgende: q2-run + `payoutBuffer` van constante naar input. ⚠️ **INGETROKKEN 24-08** — zelfde reden als D-45. Hiermee vervallen ook de eerste chronologische payouts die de simulatie ooit opleverde (6 payouts, $6.161, geen breach met opnamebuffer) als geldend resultaat. Ze blijven als historie in dit item en in de exports staan. |
| **D-24** | done | Pine Dev | ~~Compile-schuld op v6.9.5~~ — **VERVALLEN 24-08.** De v6.9.5-familie is vervangen door de `v1_0_0`-lijn uit `MEX_FLEET_PACKAGE_2026-08-23` (besluit Ferry). Een compile-ronde op scripts die uit de vloot gaan heeft geen waarde meer; het werk verschuift naar D-42. _(was SM-13)_ |
| **D-57** | todo | Pine Dev | **Route A: `derisk`/`deriskPA` in de bevroren configs — aparte onderzoeksronde** (besluit Ferry 25-08: B nu, A later). De `.pine`-bron schaalt contracten al in, de bevroren configs starten vanaf dag 1 op volle grootte. A is de nettere eindtoestand: één bron van waarheid, en Pine kent zijn eigen positiestand. ⚠️ **Maar A raakt bevroren parameters, en dat is per definitie een nieuwe onderzoeksronde vanaf trap 1** — geen tweak. Niet beginnen zolang **D-54** open staat: zolang de harde poorten van LEON en REY dicht moeten, is er geen geldige meetlat om een nieuwe configuratie tegen af te zetten. **Volgorde: D-54 → D-57.** _(uitgezet door Scrum Master 25-08 op besluit Ferry)_ |
| **D-25** | todo | Backtest Setup | Twee taken bouwen op weerlegde aannames: engine-spec (ingehaald door het lab) en woensdag-analyse (cherrypicking weerlegd). _(was SM-12)_ |
| **D-26** | wip | Scrum Master | **`docs/chats.md` archiveren.** Geclaimd 25-08. Eerst twee dingen redden die nergens anders staan — de CVD-regel en "geen getal zonder dataset-id" — die verhuizen naar `CLAUDE.md`. Daarna het bestand naar `docs/archive/`. |
| **D-27** | todo | Backtest Setup | `docs/state.md` is nooit ingevuld — live settings `_TBD_`, `data/manifest.json` bestaat niet. |
| **D-28** | review | Legacy (Discord Notify) | **Notify-kanaal: zes uitbreidingen.** **Gedeblokkeerd 20-08** — D-04 beslecht, het .NET-kanaal is de eigenaar, dus de 3 resterende hooks mogen gebouwd worden. Status per uitbreiding + plan in `middleware/docs/D-28-NOTIFY-CHANNEL.md`. Live (uit routed_log-bewijs): (1) rich embeds ✅ (3) P&L + MFE/MAE bij close ✅. Half: (2) per-kanaal routing — de config-tabel en pure-lookup `notify_routing.py` staat, wacht op de .NET-dispatch die hem leest; (5) failure-severity tiering ✅, rate-limit ontbreekt; (6) Telegram-pariteit — routing klaar, endpoint aan .NET-kant ontbreekt. (4) chart-snapshot = aparte D-22, niet dubbelen. **Middleware App geleverd** (19-08): env-tabel + `webhook_for()`/`describe()` + 10 tests die borgen dat debug en routing in sync blijven. **Resteert voor Legacy:** 3 kleine hooks in `Mex.Journal.Receiver` — zie doc §*Wat er voor Legacy resteert*. |
| **D-29** | done | Ferry | ~~`web/**` formeel in de eigenaarstabel~~ — **BESLIST 25-08: `web/**` is van Web.** |
| **D-30** | todo | Ferry | **Vier vervallen Notion-items verwijderen — kandidaten opgezocht 25-08.** De oorspronkelijke lijst van vier is nergens vastgelegd, dus dit zijn de items die aan het criterium voldoen: **(1)** *"✅ v7.0-FM scripts compileren in Pine Editor (8×)"* — `Someday`; v7.0-FM heeft nooit bestaan, vastgesteld bij D-24. **(2)** *"🛠️ D-06 · (samengevoegd) .NET receiver mergen naar de werkbranch"* — `Done`, maar een duplicaat naast de échte D-06 die nog `Next` staat; twee items met hetzelfde nummer. **(3)** *"⛔ VERVALLEN — MEX DEV Control Center (verplaatst naar LifeOS)"* — draagt de markering al in de titel. **(4)** onbekend — als je er een vierde herkent, weg ermee; zo niet, dan waren het er drie. ⚠️ Laat *"🛠️ D-00 · BESLUIT — drie scrum-borden samengevoegd"* **staan**: dat is historie van een geldig besluit, geen dood item. _(kandidaten opgezocht door de Scrum Master 25-08)_ |
| **D-31** | review | Scrum Master | **Runtime-waarheid: gecommitte snapshot — AKKOORD Ferry 25-08, gebouwd.** `middleware/deploy/mex-runtime-snapshot.{sh,service,timer}`: schrijft elk uur `docs/runtime-snapshot.md` met de checksum en het regelaantal van `Program.cs`, de mtime van de binary, `ActiveEnterTimestamp`, de status van vijf andere units, de unit-definitie **zonder `Environment=`-regels**, en de **namen** van gezette env-variabelen (nooit de waarden). Bevat een expliciete regel *"binary ouder dan de bron?"* — precies de vraag van 24-08. Droog getest: secret-filter houdt, en een bug gevonden en gefixt waarbij een falende `systemctl` de hele snapshot weggooide. **Resteert voor Ferry:** installeren — `install -m755 deploy/mex-runtime-snapshot.sh /usr/local/bin/`, de twee units naar `/etc/systemd/system/`, `daemon-reload`, `enable --now mex-runtime-snapshot.timer`. Draait `git push` niet vanaf de VPS, dan blijft de snapshot lokaal staan en verschijnt hij in `journalctl` — nog steeds bruikbaar. _(was S-11)_ |
| **D-32** | done | Ferry | ~~Bevoegdheid Scrum Master~~ — **beantwoord 19-08: mag zelf corrigeren.** Grenzen vastgelegd in de werkafspraken §1c. |
| **D-33** | done | Ferry | ~~Toezichtritme~~ — **beantwoord 19-08: hybride, op mutaties.** Werkwijze en watermerk vastgelegd in §1d. |
| **D-51** | todo | Pine Dev | **`f_pmtJSON` staat negen keer byte-identiek in de repo.** Vastgesteld bij D-44: het functieblok heeft in alle negen `v1_0_0`-scripts dezelfde checksum (`9812c09d5b`). Elke wijziging aan de PMT-payload kost daarmee negen bestanden én negen keer opnieuw uploaden naar TradingView, en één vergeten script geeft stille afwijking in het live executiepad — precies het risico dat D-44 zichtbaar maakte. **Voorstel:** de payload-bouwers (`f_pmtJSON`, `f_pcSym`, de middleware-alert) naar één Pine v6 `library()` en `import` in de negen scripts. ⚠️ **Niet nu:** dit raakt negen live scripts en moet daarom opnieuw langs trap 1 (Pine-pariteit) van de pijplijn. **Eerst D-44 uitleveren**, dan dit als eigen onderzoeksronde. _(gesignaleerd door Ferry 25-08 als "niet elke wijziging met de hand op alle scripts")_ |

---

## Watermerk — laatste toezichtronde

> De Scrum Master begint elke sessie met `git fetch origin --prune` en vergelijkt
> hiermee. Alles wat nieuwer is dan deze hashes is nog niet beoordeeld.
> **Werk dit blok bij aan het eind van elke ronde.**

| Branch | Beoordeeld t/m | Datum |
|---|---|---|
| `claude/middleware-setup-guide-afhvtk` | `ec981f4` | 2026-08-25 |
| `claude/legacy-accounts-scripts-analysis-ui0j6m` | `2f05103` | 2026-08-19 |
| `claude/discord-notify-hnydfa` | `5a5f49a` | 2026-08-19 |
| `claude/analyses-data-chat-org-3tii8j` | `f6e9af0` | 2026-08-19 |
| `claude/pine-dev-l410a6` | `dc567e8` | 2026-08-18 |
| `claude/mex-traders-website-ont1mk` | `9920af1` | opgenomen in de werkbranch |
| `claude/mcp-trader-dev-sse-ibl64y` | `34537f5` | dood — opgenomen in de werkbranch |
| `main` | `c767a0d` | 2026-07-29 |
