// AUTO-GENERATED from data/propfirms.json by tools/gen_pine_firms.py — DO NOT EDIT.
// Single source of truth is the JSON; re-run the generator after editing it.
//@version=6
library("PropFirms")

// rules(preset) -> [ddType, maxLoss$, goal$, dll$, consPct, acctSize$]
// ddType: "Intraday" | "EOD" | "Static". Feed these into the account inputs.
export rules(string preset) =>
    string ddType = "Intraday"
    float maxLoss = 0.0
    float goal = 0.0
    float dll = 0.0
    float consPct = 0.0
    float acctSize = 0.0
    if preset == "apex_250k_legacy_eval"
        ddType := "Intraday"
        maxLoss := 6500.0
        goal := 15000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 250000.0
    else if preset == "apex_25k_legacy_eval"
        ddType := "Intraday"
        maxLoss := 1500.0
        goal := 1500.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 25000.0
    else if preset == "apex_50k_eod_eval"
        ddType := "EOD"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 50000.0
    else if preset == "apex_50k_eod_pa"
        ddType := "EOD"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 50.0
        acctSize := 50000.0
    else if preset == "apex_50k_intraday_eval"
        ddType := "Intraday"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 50000.0
    else if preset == "apex_50k_intraday_pa"
        ddType := "Intraday"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 50.0
        acctSize := 50000.0
    else if preset == "apex_50k_legacy_eval"
        ddType := "Intraday"
        maxLoss := 2500.0
        goal := 3000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 50000.0
    else if preset == "apex_50k_legacy_pa"
        ddType := "EOD"
        maxLoss := 2500.0
        goal := 3000.0
        dll := 1000.0
        consPct := 30.0
        acctSize := 50000.0
    else if preset == "daytraders_s2f_25k_instant"
        ddType := "EOD"
        maxLoss := 1000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 50.0
        acctSize := 25000.0
    else if preset == "daytraders_static_25k_eval"
        ddType := "Static"
        maxLoss := 750.0
        goal := 2500.0
        dll := 0.0
        consPct := 0.0
        acctSize := 25000.0
    else if preset == "ftmo_100k_challenge"
        ddType := "Static"
        maxLoss := 10000.0
        goal := 10000.0
        dll := 5000.0
        consPct := 0.0
        acctSize := 100000.0
    else if preset == "fundednext_15k_challenge"
        ddType := "Static"
        maxLoss := 1500.0
        goal := 1200.0
        dll := 750.0
        consPct := 0.0
        acctSize := 15000.0
    else if preset == "fundingpips_10k_eval"
        ddType := "Static"
        maxLoss := 1000.0
        goal := 800.0
        dll := 500.0
        consPct := 0.0
        acctSize := 10000.0
    else if preset == "mffu_50k_eval"
        ddType := "EOD"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 50000.0
    else if preset == "takeprofittrader_50k_eval"
        ddType := "EOD"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 50000.0
    else if preset == "the5ers_hypergrowth_5k"
        ddType := "Static"
        maxLoss := 500.0
        goal := 400.0
        dll := 250.0
        consPct := 0.0
        acctSize := 5000.0
    else if preset == "topstep_50k_eval"
        ddType := "EOD"
        maxLoss := 2000.0
        goal := 3000.0
        dll := 1000.0
        consPct := 0.0
        acctSize := 50000.0
    else if preset == "tradeify_advanced_25k_instant"
        ddType := "EOD"
        maxLoss := 1500.0
        goal := 3000.0
        dll := 1000.0
        consPct := 50.0
        acctSize := 25000.0
    [ddType, maxLoss, goal, dll, consPct, acctSize]

// Preset keys (for the strategy's input.string options=[...]):
// "apex_250k_legacy_eval", "apex_25k_legacy_eval", "apex_50k_eod_eval", "apex_50k_eod_pa", "apex_50k_intraday_eval", "apex_50k_intraday_pa", "apex_50k_legacy_eval", "apex_50k_legacy_pa", "daytraders_s2f_25k_instant", "daytraders_static_25k_eval", "ftmo_100k_challenge", "fundednext_15k_challenge", "fundingpips_10k_eval", "mffu_50k_eval", "takeprofittrader_50k_eval", "the5ers_hypergrowth_5k", "topstep_50k_eval", "tradeify_advanced_25k_instant"
